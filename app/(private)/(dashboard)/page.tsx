import UserBadge from "@/src/(public)/(login)/authentication/components/UserBadge";

function Dashboard() {
    return (
        <main>
            <UserBadge />
            <h1>Dashboard</h1>
        </main>
    );
}

export default Dashboard;